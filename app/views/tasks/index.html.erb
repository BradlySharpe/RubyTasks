<h1>Tasks</h1>
<%= form_for Task.new do |f| %>
  <%= f.label :task %>
  <%= f.text_field :task %>
  <%= f.submit %>
<ul>
  <% for task in @tasks %> <!-- @tasks is from controller -->
    <li><%= task.task %></li>
  <% end %>
</ul>
